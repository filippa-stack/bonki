

## Replace Still Us illustration with new asset

Copy the uploaded image to both asset paths that the codebase already imports:

1. **`src/assets/illustration-still-us-home.png`** — used by product manifest, home screen, category header, intro screen
2. **`src/assets/illustration-still-us-tile.png`** — used by library tile

No code changes needed — same filenames, transparent background works perfectly against the dark product backgrounds.

