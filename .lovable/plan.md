

## Center Vårt Vi tile illustration horizontally

### Change in `src/components/ProductLibrary.tsx` (line 1001)

Change `objectPosition` from `'center bottom'` to `'center center'`.

This single change centers the couple both horizontally and vertically within the container, matching how kids product creatures are positioned. No other changes needed.

