

## Two Copy Changes

### Change 1 — `src/data/productIntros.ts`
Update the `jag_i_mig` body text: add "frågan" before "hur var din dag" and remove "Utan den riktiga. "

### Change 2 — `src/components/ProductLibrary.tsx`
Replace subtitle `'Verktyg för samtalen som inte blir av'` with `'För samtalen som vill bli av'`

Two files, two string replacements, no logic or styling changes.

