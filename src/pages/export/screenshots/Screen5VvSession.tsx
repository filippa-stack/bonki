/**
 * Screen 5 — Vårt Vi session. Renders real CardView for an expressing-needs
 * card mid-session.
 */
import RealAppFrame from './RealAppFrame';

export default function Screen5VvSession() {
  return <RealAppFrame src="/card/expressing-needs?demo=1&devState=browse&step=1" />;
}
